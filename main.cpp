#include <iostream>
#include <vector>
#include <limits>
#include "heap.h"
#include "util.h"

// Constants
const int N = 6;
const int CAPACITY = 20;

int main(){
    // Assign Values in Vector
    
    
    // // Create Heap from Vector
    // buildHeap(&heap, V, N);
    // printHeap(&heap);
    
    return 0;
}